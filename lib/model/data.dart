class Model {
  // ignore: non_constant_identifier_names
  String? image, title, discription, price, quantity, rating;
  Model(
      {this.image,
      this.title,
      this.discription,
      this.price,
      this.quantity,
      this.rating,
      });
}
